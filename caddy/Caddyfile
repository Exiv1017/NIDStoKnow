{ 
  email {$ACME_EMAIL}
}

{$DOMAIN} {
  encode gzip
  # Proxy all to the frontend container (Nginx) inside the compose network
  reverse_proxy frontend:80
}

# Redirect www to apex
www.{$DOMAIN} {
  redir https://{$DOMAIN}{uri} permanent
}
